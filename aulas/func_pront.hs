{-
	++ concatena duas listas
	null [] checa se a lista ta vazia
	tail retorna a calda de uma lista
	last retorna o ultimo elemento de uma lista
	init devolve uma lista sem o ultimo elemento
	head retorna a cabeca de uma lista
	length retorna o tamanho da lista
	drop remove os n primeiros elementos de uma lista
	lines devolve uma lista de lista
	take pega os n primeiros elementos de uma lista
	words devolve uma lista de palavras de string
	reverse retorna lista reversa
	repeate cria uma lista infinita retornando o dado de entrada
	replicate reprona a replicacao do dado
	cycle repete a string infinitamente
	splitAt split na posicao
	:t :type checa o cabecalho
	and retorna o and
	or retorna o or
	not retorna o not
	zip cria uma tupla de duas listas de cada posicao
	unzip reverso do zip
	
-}
